export type PropColorTheme = {
  colorTheme: string
}
export interface UserEmail {
  email: string;
}

export interface Likes {
  id: string;
  name: string;
  icon: string;
  user: any;
  heart?: any
}